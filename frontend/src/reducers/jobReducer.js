import {
  ADD_JOBS_,
  USER_LOADING
} from "../actions/types"

const initialState = {
  jobs: [],
  job: {}
};

export default function(state = intialState, action){
    switch(action.type) {
        default: 
            return state;
    }
}